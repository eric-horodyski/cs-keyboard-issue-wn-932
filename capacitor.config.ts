import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.keyboard.issue',
  appName: 'cs-keyboard-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
